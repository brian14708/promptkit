{
  "name": "isola-core",
  "version": "0.5.0",
  "description": "Node.js SDK for Isola sandboxed code execution",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "napi": {
    "binaryName": "isola",
    "npmClient": "npm",
    "targets": [
      "x86_64-unknown-linux-gnu",
      "aarch64-unknown-linux-gnu",
      "aarch64-apple-darwin",
      "x86_64-pc-windows-msvc"
    ]
  },
  "scripts": {
    "build": "pnpm run tsc && napi build --release --platform --js isola.js --dts isola.d.ts --manifest-path ../Cargo.toml --output-dir dist",
    "lint": "biome check src tests && tsc --noEmit -p tsconfig.json",
    "tsc": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@napi-rs/cli": "^3.9.0",
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  },
  "packageManager": "pnpm@12.3.1",
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/brian14708/isola"
  },
  "dependencies": {
    "cbor-x": "^1.6.6"
  }
}
